import 'package:flutter/material.dart';

class Seekhna extends StatelessWidget {
  const Seekhna({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    );
  }
}
